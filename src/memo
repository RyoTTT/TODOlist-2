        {todoList.map((todo:todos)=>{
          <Box key={todo.id}>
          <ListItem>{todo.text}</ListItem>
          <Checkbox>完了したらチェック</Checkbox>
          </Box>
        })}

        const resetTodoLists = todoList2.filter((todo:any)=> typeof todo.state === "number");